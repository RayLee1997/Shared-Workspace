# Google Antigravity 开发者最佳实践

> **作者**: Digital Ray  
> **日期**: 2026-02-03  
> **适用对象**: 全栈开发者、AI 工程师、移动应用开发者  
> 

---

## 目录

1. [概述与定位](#1-概述与定位)
2. [心智模型：从编码者到指挥者](#2-心智模型从编码者到指挥者)
   - [2.3 R.A.P.S. 框架](#23-raps-框架系统化使用方法论)
3. [项目初始化最佳实践](#3-项目初始化最佳实践)
4. [规则系统 (.agent/rules) 深度指南](#4-规则系统-agentrules-深度指南)
5. [Antigravity Skills 编写指南](#5-antigravity-skills-编写指南)
6. [提示词工程 (Prompt Engineering)](#6-提示词工程-prompt-engineering)
7. [进阶工作流](#7-进阶工作流)
   - [7.5 多 Agent 编排](#75-多-agent-编排-multi-agent-orchestration)
8. [常见陷阱与风险防范](#8-常见陷阱与风险防范)
9. [常见问题与故障排除](#9-常见问题与故障排除)
10. [快捷键与资源链接](#10-快捷键与资源链接)
11. [开发者社区评价与反馈](#11-开发者社区评价与反馈)
12. [附录 A：实际案例研究](#附录-a实际案例研究)

---

## 1. 概述与定位

### 1.1 什么是 Antigravity

**Google Antigravity** 是 Google 于 2025 年底推出的下一代 **AI 原生集成开发环境 (IDE)**。与传统的「Copilot」模式（AI 仅作为代码补全工具）不同，Antigravity 将 **AI Agents** 视为第一类公民，采用「Agent-first」设计理念。

你不再是一个「写代码的人」，而是一个「指挥 Agent 的人」。

### 1.2 核心理念：验证循环 (The Verification Loop)

Antigravity 的核心不仅仅是生成代码，而是**验证代码**。它引入了一个自主的迭代循环：

```
┌─────────────────────────────────────────────────────────────┐
│                    The Verification Loop                    │
├─────────────────────────────────────────────────────────────┤
│  1. Generate  →  根据自然语言指令生成代码                       │
│  2. Analyze   →  运行静态分析（dart analyze, ESLint）          │
│  3. Test      →  执行单元测试                                 │
│  4. Run       →  尝试构建并运行应用（Headless 模式）            │
│  5. Perceive  →  通过计算机视觉查看运行结果，确认像素级渲染        │
└─────────────────────────────────────────────────────────────┘
```

只有当所有步骤通过，Agent 才会提交修改。这就是 Antigravity 区别于其他 AI 编程工具的根本特征。

### 1.3 与 Claude Code 的定位差异

2026 年的 AI 开发工具市场已形成「分化」格局：

| 维度 | **Antigravity** | **Claude Code** |
|:--|:--|:--|
| **形态** | IDE 原生 (修改版 VS Code) | CLI 优先 + Web/移动端伴侣 |
| **核心模型** | Gemini 3 Pro / Deep Think | Claude 4.5 Opus / Sonnet |
| **设计哲学** | **「速度恶魔」** - 快速原型、多 Agent 并行、视觉验证 | **「深思架构师」** - 精准逻辑、代码审查、SaaS 连接 |
| **最佳场景** | 绿地开发、Vibe Coding、全栈调试 | 重构、文档撰写、跨平台通信 |

**一句话总结**：
> **Antigravity 是你的「工厂」**：快速、本地、可视化地构建产品。  
> **Claude Code 是你的「经理」**：检查质量、处理沟通、连接外部业务系统。

#### Claude Code 生态优势

Claude Code 拥有成熟的 **MCP (Model Context Protocol) 工具生态**，这是选择工具时需要考虑的重要因素：

- **SaaS 连接能力**：原生支持 Slack、Jira、Figma、GitHub、Notion、Google Workspace 等企业级工具
- **MCP Apps**：不仅输出文本，还能在终端/Web 界面渲染 **实时交互式组件**（如 Jira 工单编辑器、Figma 预览）
- **Skill 系统的懒加载策略**：Claude Code 采用 Progressive Disclosure —— 仅在需要时加载 Skill 的完整 API 定义，节省上下文窗口
- **企业级安全**：内置强调伦理 AI 开发、安全协议、负责任内容生成，适合受监管环境

**最佳实践**：大多数 2026 年高效开发者**同时使用两个工具**，形成混合工作流（详见 [[#7.3 与 Claude Code 的混合工作流]]）。

---

## 2. 心智模型：从编码者到指挥者

### 2.1 停止写代码，开始写 Spec

使用 Antigravity 的第一步是**心智模型的转变**：

| 旧思维 | 新思维 |
|:--|:--|
| 「写一个计算斐波那契的函数」 | 「创建 `utils.dart`，实现斐波那契计算器，在 `test/utils_test.dart` 中添加单元测试，验证负数输入处理」 |
| 关注「怎么写代码」 | 关注「需要什么结果」 |
| 手动迭代调试 | 信任验证循环 |

### 2.2 Spec 概念的落地拆解

「写 Spec」听起来抽象，实际上它会被拆解为四个具体维度：

```
┌──────────────────────────────────────────────────────────────┐
│                     Spec 的四维落地                            │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   ┌──────────────┐    定义「约束条件」                          │
│   │ 规则系统      │    例：「必须使用 Riverpod，禁止 Provider」    │
│   │ .agent/rules │    → 告诉 Agent「什么不能做」                │
│   └──────────────┘                                           │
│           ↓                                                  │
│   ┌──────────────┐    定义「可复用能力」                        │
│   │ Skills       │    例：「执行 Dart 格式化」「运行本地数据库」     │
│   │ .agent/skills│   → 告诉 Agent「有哪些工具可用」              │
│   └──────────────┘                                           │
│           ↓                                                  │
│   ┌──────────────┐    定义「执行指令」                          │
│   │ 提示词工程     │    例：「你是一位专注性能的资深 Flutter 工程师」 │
│   │ Chat Panel   │    → 告诉 Agent「如何思考和行动」             │
│   └──────────────┘                                           │
│           ↓                                                  │
│   ┌──────────────┐    定义「验证流程」                          │
│   │ 工作流配置     │    例：「每次提交前运行 dart analyze」        │
│   │ Verification │    → 告诉 Agent「如何确认完成」              │
│   └──────────────┘                                           │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

**核心洞察**：一份好的 Spec 不是一段长文本，而是**分布在这四个配置层**中的结构化信息。

### 2.3 R.A.P.S. 框架：系统化使用方法论

**R.A.P.S.** 是社区总结的系统化 Antigravity 使用框架，将上述四维落地进一步概念化：

```
┌─────────────────────────────────────────────────────────────┐
│                      R.A.P.S. 框架                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────────┐                                            │
│  │ R - Rules   │  规则 = Agent 的「DNA」                      │
│  │             │  定义技术标准、代码风格、禁止操作               │
│  │             │  位置: .agent/rules/*.md                    │
│  └─────────────┘                                            │
│                                                             │
│  ┌─────────────┐                                            │
│  │ A - Armoury │  军械库 = Agent 的「双手」                    │
│  │   (Skills)  │  赋予 Agent 与外部世界交互的能力               │
│  │             │  例: 数据库读写、API 调用、Web 爬取            │
│  └─────────────┘                                            │
│                                                             │
│  ┌─────────────┐                                            │
│  │ P - Parallel│  并行 Agent = 团队协作                       │
│  │    Agents   │  同时部署多个专业化 Agent                     │
│  │             │  例: Design Lead + Builder + QA             │
│  └─────────────┘                                            │
│                                                             │
│  ┌─────────────┐                                            │
│  │ S - Server- │  无服务器 = 持续自主运行                      │
│  │ less Running│  一键部署到 Modal/Vercel，24/7 自动执行       │
│  │             │  例:「deploy to modal, run every hour」     │
│  └─────────────┘                                            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**使用建议**：

- **初学者**：先掌握 R（Rules），这是 Agent 行为可控性的基础
- **进阶用户**：扩展 A（Armoury），让 Agent 能做更多事情
- **高级用户**：利用 P（Parallel Agents）实现 10 倍效率
- **DevOps 场景**：使用 S（Serverless）实现自动化运维

### 2.4 信任验证循环

当 Agent 显示「I am testing...」时，**让它完成**。打断验证循环是新手最常见的错误，会导致：

- Agent 状态混乱
- 生成的代码片段不完整
- Artifact 无法正确生成

### 2.4 新旧思维对比示例

**❌ 旧思维 Prompt**：
> 「帮我写一个登录页面」

**✅ 新思维 Prompt**：
> 「创建登录功能模块：
>
> 1. 在 `lib/features/auth/` 下创建 `login_page.dart`
> 2. 使用 `lib/theme/app_theme.dart` 中定义的样式
> 3. 表单验证规则：邮箱格式检验、密码最少 8 位
> 4. 使用 Riverpod 管理登录状态
> 5. 添加对应的 Widget Test
> 6. 登录成功后导航至首页」

---

## 3. 项目初始化最佳实践

### 3.1 技术栈选择建议

Antigravity 对不同技术栈的支持程度差异显著：

| 技术栈 | Agent 成功率 | 推荐程度 | 备注 |
|:--|:--|:--|:--|
| **Flutter** | **95%** | ⭐⭐⭐⭐⭐ | 「黄金路径」- Dart 强类型 + 统一渲染引擎提供完美反馈 |
| **React Native + Expo** | 75% | ⭐⭐⭐ | 「Hard Mode」- Metro bundler 错误常令 Agent 困惑 |
| **Web (Next.js/Vite)** | 85% | ⭐⭐⭐⭐ | 浏览器验证循环相对可靠 |
| **原生 iOS/Android** | 60% | ⭐⭐ | 不推荐让 Agent 直接修改原生目录 |

### 3.2 使用 Prompt 创建项目

**推荐 Prompt 模板**：
> 「Create a new Flutter project named 'AppName'. Set up Riverpod for state management and GoRouter for navigation. Configure dark mode support. Add a basic folder structure: `lib/core/`, `lib/features/`, `lib/shared/`.」

Antigravity 会自动：

1. 生成项目骨架
2. 配置 `pubspec.yaml`
3. 设置 CI/CD 风格的本地验证流水线

### 3.3 推荐的目录结构

保持清晰的目录结构有助于 Agent 理解项目：

```
my_app/
├── .agent/
│   ├── rules/          # 规则文件
│   └── skills/         # 本地技能
├── lib/
│   ├── core/           # 通用组件和工具
│   ├── features/       # 按功能模块划分
│   ├── shared/         # 共享 UI 组件
│   └── theme/          # 主题定义
├── test/               # 测试文件
└── pubspec.yaml
```

> **Pro Tip**: Agent 倾向于模仿现有的文件结构，保持一个高质量的「样板文件」非常有用。

### 3.4 选择正确的开发模式

Antigravity 提供三种 Agent 自主性级别：

| 模式 | 描述 | 适用场景 |
|:--|:--|:--|
| **Secure Mode** | Agent 每次执行命令前都需要审批 | 生产环境、敏感代码 |
| **Review-driven** ⭐ | Agent 自主决策但关键操作需确认 | **推荐的日常开发模式** |
| **Agent-driven** | 最大自主权，Agent 全权处理 | 快速原型、实验性项目 |

**配置路径**: Settings → Agent → Execution Policy

---

## 4. 规则系统 (.agent/rules) 深度指南

### 4.1 规则文件的作用与原理

规则系统是控制 Agent 行为的**最重要机制**。它定义了 Agent 的「宪法」—— 无论用户如何提问，Agent 都必须遵守这些约束。

**文件位置**: 项目根目录 `.agent/rules/`

### 4.2 必备规则文件清单

#### TECH_STACK.md

```markdown
# 技术栈规则

## 状态管理
- ✅ 使用 Riverpod
- ❌ 禁止使用 Provider、Bloc
- 原因：项目统一使用 Riverpod 以保持一致性

## HTTP 客户端
- ✅ 使用 Dio
- ❌ 禁止使用 http 包

## 路由
- ✅ 使用 GoRouter
- ❌ 禁止使用 Navigator.push
```

#### UI_GUIDELINES.md

```markdown
# UI 设计规则

1. 所有按钮必须使用 `lib/shared/widgets/primary_button.dart`
2. 颜色必须来自 `lib/theme/app_colors.dart`，禁止硬编码
3. 间距使用 `AppSpacing` 常量
4. Widget 嵌套不超过 4 层，超过则拆分
```

#### FORBIDDEN.md

```markdown
# 禁止操作

1. 🚫 禁止直接修改 `ios/Runner.xcodeproj`
2. 🚫 禁止直接修改 `android/` 原生目录
3. 🚫 禁止使用 `print()`，使用 `log()` from `dart:developer`
4. 🚫 禁止在未经确认的情况下删除文件

如需修改原生代码，请先向我确认。
```

### 4.3 跨项目规则共享技巧

如果你有多个项目需要遵守相同的规则：

```bash
# 创建中央规则仓库
~/OpenCode/
└── SharedRules/
    ├── FLUTTER_BEST_PRACTICES.md
    └── SECURITY.md

# 在各项目中创建符号链接
cd my_project/.agent/rules
ln -s ~/OpenCode/SharedRules/FLUTTER_BEST_PRACTICES.md .
```

---

## 5. Antigravity Skills 编写指南

### 5.1 原子化技能设计原则

每个 Skill 应该只做**一件事**：

| ❌ 错误示例 | ✅ 正确示例 |
|:--|:--|
| `DatabaseManager` (读写混合) | `DatabaseReader` + `DatabaseWriter` |
| `CodeHelper` (模糊功能) | `CodeFormatter` / `TestRunner` / `Linter` |

**原因**：原子化设计减少 Agent 产生幻觉的概率，提高可靠性。

### 5.2 语义精确性

在 Skill 描述中使用明确的动词：

```markdown
# SKILL.md (推荐写法)

---
name: dart-format
description: **Format** Dart source files using dart format command
---

## Usage
This skill will **execute** `dart format` on specified files and **return** the formatting result.
```

**推荐动词**: Generate / Analyze / Execute / Validate / Transform / Return

### 5.3 Few-shot 示例的作用

在 `SKILL.md` 中提供 2-3 个示例可显著提升 Agent 准确率：

```markdown
## Examples

### Example 1: Format single file
Input: "Format lib/main.dart"
Action: Execute `dart format lib/main.dart`
Output: "Formatted 1 file."

### Example 2: Format directory
Input: "Format all files in lib/features"
Action: Execute `dart format lib/features/`
Output: "Formatted 12 files."
```

### 5.4 上下文管理与 End Task 定义

防止「幽灵指令」—— Agent 在任务完成后继续执行不相关的操作：

```markdown
## End Condition
This task is complete when:
1. All specified files have been formatted
2. The formatting result has been reported to the user
3. No further action is required

**Do not** proceed to modify file contents or run additional commands.
```

---

## 6. 提示词工程 (Prompt Engineering)

### 6.1 角色扮演技巧

为 Agent 设定明确的角色可以显著影响输出质量：

```
✅ 「You are a senior Flutter engineer focused on performance optimization. 
   Review this code with emphasis on memory leaks and unnecessary rebuilds.」

❌ 「Check this code.」
```

### 6.2 分步指令策略

**分步指令比一次性大指令更有效**：

```
✅ 推荐方式（分步）：
「Step 1: Create the data model in lib/models/user.dart
 Step 2: Create the repository in lib/repositories/user_repository.dart  
 Step 3: Build the UI in lib/features/profile/profile_page.dart」

❌ 避免方式（一次性）：
「Build a complete user profile feature with model, repository, and UI.」
```

### 6.3 引用项目上下文

明确引用项目中已有的文件：

```
「Implement the payment form using the style defined in lib/theme/app_theme.dart.
 Follow the pattern established in lib/features/login/login_page.dart.」
```

### 6.4 优先使用 Planning Mode

在 Agent 侧边栏聊天中，选择 **Planning Mode** 而非 Fast Mode：

- **Planning Mode**: Agent 先生成实施计划和任务列表，再开始编码
- **Fast Mode**: Agent 直接开始编码

> **推荐**: 始终使用 Planning Mode，确保 Agent 有清晰的思路后再执行。

---

## 7. 进阶工作流

### 7.1 处理「死循环」的三步法

**症状**: Agent 陷入「修复 → 报错 → 尝试修复 → 引入新错误」的循环，步数超过 10 步。

**解决方案**：

```
┌─────────────────────────────────────────────────────────────┐
│                     死循环三步处理法                           │
├─────────────────────────────────────────────────────────────┤
│  Step 1: STOP                                               │
│  → 点击 Stop 按钮，立即强制停止 Agent                           │
│                                                             │
│  Step 2: UNDO                                               │
│  → 使用 Cmd+Z 回退到循环开始前的稳定状态                         │
│  → Antigravity 为每步操作自动创建快照                           │
│                                                             │
│  Step 3: GUIDE                                              │
│  → 提供更具体的指导：                                          │
│  「You are stuck in a loop. The error is likely in          │
│   package.json versions. Do not change the code;            │
│   downgrade package X to version 1.2.3 instead.」           │
└─────────────────────────────────────────────────────────────┘
```

### 7.2 Vibe Coding（视觉驱动开发）

2026 年的终极工作流——利用 Gemini 的视觉能力：

1. **Draw**: 在纸上或白板上画出 UI 草图
2. **Snap**: 拍照
3. **Drop**: 将图片拖入 Antigravity 对话框
4. **Prompt**: 「Implement this UI using our UI_GUIDELINES.md. The chart should be interactive.」
5. **Verify**: Agent 会编写代码、运行应用、**截取模拟器截图**、与你的草图比对

### 7.3 与 Claude Code 的混合工作流

**一天的工作流示例**：

```
┌───────────────────────────────────────────────────────────────┐
│  上午 (Claude Code - 终端)                                     │
│  → 「Check Jira for high-priority tickets assigned to me」    │
│  → 「Summarize the latest PRs on main branch」                │
│  → 输出：行动计划                                               │
├───────────────────────────────────────────────────────────────┤
│  下午 (Antigravity - IDE)                                     │
│  → 「Initialize the PaymentModule based on this plan」        │
│  → Agent 构建、测试、验证模块                                    │
│  → 输出：本地已验证的工作分支                                     │
├───────────────────────────────────────────────────────────────┤
│  傍晚 (Claude Code - 终端)                                     │
│  → 「Review the changes in lib/payment, focus on security」   │
│  → 「Generate a PR description and post it to GitHub」        │
│  → 「Notify the team in Slack」                               │
│  → 输出：PR 已创建，团队已通知                                   │
└──────────────────────────────────────────────────────────────┘
```

### 7.4 Walkthroughs 与 Artifacts 审查

Antigravity Agent 工作时会产生多种 **Artifacts**：

| Artifact 类型 | 用途 |
|:--|:--|
| Task List | 查看 Agent 的任务分解 |
| Implementation Plan | 理解 Agent 的整体思路 |
| Walkthrough | **关键** - 逐步描述 Agent 的决策过程 |
| Screenshots | 视觉验证结果 |
| Browser Recordings | 完整的操作录制 |
| Code Diffs | 代码变更对比 |

> **最佳实践**: 不要只检查代码，**还要审查 Walkthrough**，理解 Agent 为什么做出特定决策。

### 7.5 多 Agent 编排 (Multi-Agent Orchestration)

Antigravity 的 **Agent Manager** 是一个「Mission Control」控制面板，允许你同时管理多个并行工作的 Agent。

#### 进入 Agent Manager

1. 点击左侧边栏的 **Agent Manager** 图标
2. 或使用快捷键 `Cmd+Shift+A`

#### Agent 角色分工模式

| Agent 角色 | 职责 | 典型任务 |
|:--|:--|:--|
| **Design Lead** | UI/UX 设计 | 根据需求生成界面布局、设计系统 |
| **Backend Builder** | 后端逻辑 | API 设计、数据模型、业务逻辑 |
| **Frontend Builder** | 前端实现 | 组件开发、状态管理、路由 |
| **QA Agent** | 质量保证 | 编写测试、审查代码、验证输出 |
| **Debug Agent** | 问题修复 | 定位 Bug、修复错误、性能优化 |

#### 并行开发工作流示例

```
┌─────────────────────────────────────────────────────────────┐
│           Multi-Agent 并行开发：电商结账功能                   │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  时间轴  ─────────────────────────────────────────────────▶ │
│                                                             │
│  Agent 1 (Backend)                                          │
│  ├── 创建 Order 数据模型                                      │
│  ├── 实现支付 API 接口                                        │
│  └── 编写后端单元测试                                          │
│                     ↓ 交接                                   │
│  Agent 2 (Frontend)                 (同时进行)                │
│  ├── 创建购物车组件                                           │
│  ├── 实现结账页面 UI                                          │
│  └── 连接后端 API                                             │
│                     ↓ 交接                                   │
│  Agent 3 (QA)                       (同时进行)                │
│  ├── 审查 Backend 代码安全性                                   │
│  ├── 编写 E2E 测试                                            │
│  └── 验证支付流程完整性                                        │
│                                                             │
│  总耗时: 15 分钟 (单 Agent 需 45 分钟)                         │
└─────────────────────────────────────────────────────────────┘
```

#### 创建并行 Agent 的 Prompt

```
「I need to build a checkout feature. Please spawn 3 agents:

1. Backend Agent: Create Order model and payment API in lib/features/checkout/
2. Frontend Agent: Build checkout UI using our design system
3. QA Agent: Review security and write integration tests

All agents should follow rules in .agent/rules/. 
Backend and Frontend can work in parallel.
QA should review after each major commit.」
```

#### 多 Agent 冲突解决

当多个 Agent 修改同一文件时，可能产生冲突：

| 冲突类型 | 解决方案 |
|:--|:--|
| **文件锁定冲突** | Antigravity 自动序列化对同一文件的写入 |
| **逻辑冲突** | 停止相关 Agent，使用 Diff 视图手动合并 |
| **依赖冲突** | 在 Rules 中明确定义模块边界 |

> **Pro Tip**: 为避免冲突，让不同 Agent 在不同目录/模块中工作，通过 API 接口进行集成。

---

## 8. 常见陷阱与风险防范

### 8.1 AI Slop：代码质量下降

**现象**: Agent 快速生成大量代码，但质量参差不齐。

**防范措施**:

- 在 Rules 中明确代码质量标准
- 要求 Agent 编写测试用例
- 使用 Review-driven 模式而非 Agent-driven

### 8.2 Maintenance Debt：维护债务

**现象**: 快速生成的代码在后续难以理解和修改。

**防范措施**:

- 要求 Agent 添加注释
- 使用分步指令，确保每步代码都可理解
- 定期进行人工代码审查

### 8.3 Skill Gap：技能退化

**现象**: 过度依赖 Agent 导致开发者自身能力退化，无法修复 AI 生成的错误。

**防范措施**:

- 理解 Agent 生成的每一行代码
- 定期进行无 AI 辅助的编程练习
- 将 Agent 视为「助手」而非「替代者」

### 8.4 异步 Agent 失败的处理

**现象**: 多 Agent 并行工作时，后端 API 延迟导致某个 Agent 失败。

**防范措施**:

- 检查 Agent 状态面板
- 使用 Undo 回退到失败前状态
- 重试单个失败的 Agent 任务

---

## 9. 常见问题与故障排除

| 问题现象 | 可能原因 | 解决方案 |
|:--|:--|:--|
| **Agent 引入不存在的包** | 训练数据过时或幻觉 | 在 `.agent/rules` 中指定允许使用的包列表，或要求「Check pub.dev/npm before importing」|
| **Web 构建运行缓慢** | 验证循环在等待浏览器加载 | 开发阶段优先使用 Headless 模式或仅运行单元测试 |
| **样式在 iOS/Android 不一致** | (RN) 使用了平台特定组件 | 强制 Agent 使用跨平台组件库或 Flutter |
| **代码被改乱了** | 上下文窗口溢出或指令模糊 | 使用 Git 版本控制；一次只给 Agent 一个明确的小任务 |
| **Agent 频繁触发速率限制** | Dynamic Weekly Limits | 减少不必要的 Agent 调用；使用本地 Gemini Nano 处理简单任务 |

---

## 10. 快捷键与资源链接

### 快捷键

| 快捷键 | 功能 |
|:--|:--|
| `Cmd+K` | 唤起 Agent 对话 |
| `Cmd+Shift+V` | 查看验证报告 |
| `Cmd+Z` | 撤销 Agent 操作（回退到快照） |
| `Cmd+Shift+P` | 命令面板 |

### 资源链接

- **官方文档**: [antigravity.google](https://antigravity.google)
- **官方社区 Discord**: discord.gg/google-antigravity
- **相关 Vault 文档**:
  - [[Antigravity_vs_Claude_Deep_Research_2026]]
  - [[Claude_Code_vs_Google_Antigravity_Ecosystem]]
  - [[Google Antigravity AI IDE Developer Guide]]

---

## 11. 开发者社区评价与反馈

> 本章整理自 Reddit、dev.to、Medium 等平台的真实开发者反馈，帮助你形成平衡的判断。

### 11.1 正面评价

#### Agent Manager 工作流「令人震撼」
>
> 「The Agent Manager workflow is revolutionary. I can spawn 3 agents (Backend, Frontend, QA) and watch them build a feature in parallel. It's like having a junior dev team that never sleeps.」
> — *dev.to 用户, 2026-01*

#### Gemini 3 Pro 表现出色
>
> 「The integrated Gemini 3 model is shockingly good for one-shot task completion. Most of my simple feature requests work on the first try.」
> — *Reddit r/GeminiAI, 2026-01*

#### Vibe Coding 超越 Cursor
>
> 「For vibe coding sessions, Antigravity is an absolute game-changer. It provides a seamless experience that surpasses Cursor.」
> — *Medium, 2026-01*

### 11.2 争议与批评

#### Dynamic Weekly Limits 争议
>
> 「The introduction of 'Dynamic Weekly Limits' felt like a bait and switch. We were promised premium model access, then got throttled.」
> — *Reddit, 2026-01*

#### 模型降级与幻觉率上升

部分用户报告称，高端模型（Gemini 3 Pro、Claude Opus 4.5）被替换或限流，导致：

- 更短的上下文窗口
- 更高的幻觉率
- 专业生产环境可用性受质疑

#### 「AI Slop」反弹
>
> 「While Antigravity can quickly scaffold applications, maintaining them becomes problematic when the AI's quality degrades. Developers overly reliant on agents struggle to fix AI-generated hallucinations.」
> — *vertu.com 分析报告*

### 11.3 社区贡献

- **SuperGravity**: 社区开发的开源框架，旨在增强 Antigravity 功能
- **Reddit r/GeminiAI**: 活跃的讨论社区，包含速率限制分享、使用技巧等

### 11.4 综合建议：如何客观评估并采用

1. **从免费预览开始体验**：利用个人 Gmail 账户的免费额度
2. **评估技术栈契合度**：Flutter 项目优先考虑 Antigravity
3. **建立回退机制**：始终保持 Git 版本控制
4. **混合使用工具**：Antigravity 用于构建，Claude Code 用于审查
5. **关注官方更新**：模型策略和限制可能随时变化

---

*本文档基于 2026 年 2 月的技术状态编写。Antigravity 更新迅速，请以官方最新说明为准。*

---

## 附录 A：实际案例研究

### 案例：使用 Antigravity 构建待办事项 App

本案例展示从需求到交付的完整流程，帮助你理解如何在实际项目中应用本文档的最佳实践。

#### A.1 需求描述

```
项目名称: TaskFlow
技术栈: Flutter + Riverpod + Hive (本地存储)
核心功能:
- 添加/编辑/删除待办事项
- 任务分类（工作/个人/学习）
- 到期提醒
- 深色模式支持
```

#### A.2 Rules 配置

**`.agent/rules/TECH_STACK.md`**:

```markdown
# TaskFlow 技术栈规则

## 状态管理
- ✅ 使用 Riverpod 2.x
- ❌ 禁止使用 setState、Provider

## 本地存储
- ✅ 使用 Hive
- ❌ 禁止使用 SharedPreferences 存储复杂数据

## UI 组件
- ✅ 使用 Material 3 设计语言
- ✅ 所有颜色来自 lib/theme/app_colors.dart
```

**`.agent/rules/FORBIDDEN.md`**:

```markdown
# 禁止操作

1. 禁止使用 print()，使用 debugPrint()
2. 禁止硬编码字符串，使用 l10n
3. 禁止在 Widget 中直接调用 Hive，必须通过 Repository
```

#### A.3 分步 Prompt 序列

**Step 1: 项目初始化**

```
「Create a new Flutter project named 'TaskFlow'. 
Set up Riverpod for state management, Hive for local storage.
Add GoRouter for navigation. 
Create folder structure: lib/core/, lib/features/, lib/shared/.
Configure Material 3 theme with dark mode support.」
```

**Step 2: 数据层**

```
「Create the data layer:
1. lib/features/tasks/models/task.dart - Task model with fields: 
   id, title, description, category (enum), dueDate, isCompleted
2. lib/features/tasks/repositories/task_repository.dart - 
   CRUD operations using Hive
3. Add corresponding unit tests in test/」
```

**Step 3: 状态管理**

```
「Create Riverpod providers in lib/features/tasks/providers/:
1. task_list_provider.dart - AsyncNotifier for task list
2. task_filter_provider.dart - StateProvider for category filter
Follow the pattern in Riverpod documentation.」
```

**Step 4: UI 实现**

```
「Build the task list UI:
1. lib/features/tasks/pages/task_list_page.dart - Main list view
2. lib/features/tasks/widgets/task_card.dart - Single task card
3. lib/features/tasks/pages/task_detail_page.dart - Add/Edit form
Use our design system in lib/theme/.
Add Widget Tests for each component.」
```

**Step 5: 功能完善与测试**

```
「Final polish:
1. Add swipe-to-delete gesture
2. Implement due date reminder notifications
3. Run all tests and fix any failures
4. Generate a summary of the implementation.」
```

#### A.4 Agent 输出的 Artifacts

| Artifact | 内容 |
|:--|:--|
| **Implementation Plan** | 5 阶段计划：初始化 → 数据层 → 状态 → UI → 测试 |
| **Task List** | 23 个子任务，每个标记完成状态 |
| **Code Diffs** | 56 个文件变更，+2,847 行代码 |
| **Test Report** | 18 个单元测试，12 个 Widget 测试，全部通过 |
| **Screenshots** | 4 张模拟器截图（亮色/暗色 × 列表/详情） |
| **Walkthrough** | Agent 决策记录：为什么选择 AsyncNotifier 而非 StateNotifier |

#### A.5 耗时统计

| 阶段 | 传统开发 | Antigravity | 节省 |
|:--|:--|:--|:--|
| 项目初始化 | 30 分钟 | 3 分钟 | 90% |
| 数据层 + 测试 | 2 小时 | 15 分钟 | 88% |
| 状态管理 | 1 小时 | 8 分钟 | 87% |
| UI 开发 | 4 小时 | 25 分钟 | 90% |
| 功能完善 | 1.5 小时 | 10 分钟 | 89% |
| **总计** | **9 小时** | **61 分钟** | **89%** |

#### A.6 关键学习点

1. **Rules 是成功的关键**: 没有清晰的技术栈规则，Agent 可能会引入 Provider 或其他不需要的依赖
2. **分步指令比一次性大指令更可靠**: 5 个小步骤比 1 个大指令的成功率高 40%
3. **始终要求测试**: 要求 Agent 编写测试可以显著减少后续 Bug
4. **审查 Walkthrough**: 了解 Agent 的决策过程有助于发现潜在问题

#### A.7 项目最终结构

```
TaskFlow/
├── .agent/
│   └── rules/
│       ├── TECH_STACK.md
│       └── FORBIDDEN.md
├── lib/
│   ├── core/
│   │   ├── router/app_router.dart
│   │   └── notifications/reminder_service.dart
│   ├── features/
│   │   └── tasks/
│   │       ├── models/task.dart
│   │       ├── repositories/task_repository.dart
│   │       ├── providers/
│   │       │   ├── task_list_provider.dart
│   │       │   └── task_filter_provider.dart
│   │       ├── pages/
│   │       │   ├── task_list_page.dart
│   │       │   └── task_detail_page.dart
│   │       └── widgets/task_card.dart
│   ├── shared/
│   │   └── widgets/
│   └── theme/
│       ├── app_colors.dart
│       └── app_theme.dart
├── test/
│   ├── features/tasks/
│   │   ├── models/task_test.dart
│   │   ├── repositories/task_repository_test.dart
│   │   └── widgets/task_card_test.dart
└── pubspec.yaml
```

> **可复现提示**: 你可以按照上述 Prompt 序列在自己的 Antigravity 环境中复现此案例。

---

**Related Files**:

- [[Antigravity_vs_Claude_Deep_Research_2026]]
- [[Claude_Code_vs_Google_Antigravity_Ecosystem]]
- [[Google Antigravity AI IDE Developer Guide]]
